<?php

declare(strict_types=1);

beforeEach(function () : void
{
    //
});

it('can retrieve a single book with its author and genres', function () : void
{
    // Arrange: maak boek, auteur en genres
    // Act: GET /api/books/{id}
    // Assert: check JSON structure
});

it('can retrieve a list of books with its author and genres', function () : void
{
    // Arrange: maak meerdere boeken
    // Act: GET /api/books
    // Assert: check JSON list structure
});
